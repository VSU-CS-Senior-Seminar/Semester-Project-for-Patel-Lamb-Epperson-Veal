<!-- STYLED -->
<!-- Renders the "Post Reply to . . . -->


<!--%= render :partial => 'forem/forums/head', :locals => { :forum => @topic.forum } %-->

<!--h2>
  < %= t("forem.post.new") %>
  < % if @reply_to_post %>
    < %= render 'reply_to_post', :post => @reply_to_post %>
  < % else %>
    <em>< %= @topic.subject %></em>
  < % end %>
</h2-->

<h1><%= @topic.subject %></h1>
<br />
<div class="row">
	<div class="col-md-12">
		<% if @reply_to_post %>
			<%= render 'reply_to_post', :post => @reply_to_post %>
		<% else %>
		<% end %>
	</div>
</div>

<%= simple_form_for [forem, @topic.forum, @topic, @post] do |f| %>
	<%= render :partial => "form", :locals => { :f => f } %>
	<%= f.submit t("forem.post.buttons.reply"), :class => "btn btn-default btn-sm" %>
<% end %>